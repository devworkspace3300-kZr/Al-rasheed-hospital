DirectoryIndex index.php

<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect legacy .html URLs to .php equivalents
    RewriteCond %{THE_REQUEST} \s/+(.+?)\.html[\s?] [NC]
    RewriteRule ^ %1.php [L,R=301]
</IfModule>
